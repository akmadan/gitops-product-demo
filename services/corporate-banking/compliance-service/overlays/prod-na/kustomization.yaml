apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: corporate-banking

bases:
  - ../../base

commonLabels:
  environment: prod-na

resources:
  - rollout.yaml

patches:
  - target:
      kind: Deployment
      name: compliance-service
    patch: |-
      - op: remove
        path: /
  - target:
      kind: ConfigMap
      name: compliance-service-config
    patch: |-
      - op: add
        path: /data/ENVIRONMENT
        value: "prod-na"

---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: compliance-service
  labels:
    app: compliance-service
    strategy: canary
spec:
  replicas: 5
  selector:
    matchLabels:
      app: compliance-service
  template:
    metadata:
      labels:
        app: compliance-service
    spec:
      containers:
      - name: compliance-service
        image: docker.io/akshitmadanharness/compliance-service:latest
        ports:
        - containerPort: 8082
        env:
        - name: ENVIRONMENT
          value: "prod-na"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "1Gi"
            cpu: "800m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 5
  minReadySeconds: 30
  revisionHistoryLimit: 3
  strategy:
    canary:
      maxSurge: '25%'
      maxUnavailable: 0
      steps:
        - setWeight: 10
        - pause: { duration: 10m }
        - setWeight: 25
        - pause: { duration: 10m }
        - setWeight: 50
        - pause: { duration: 10m }
        - setWeight: 100

