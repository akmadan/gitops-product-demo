apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: corporate-banking

bases:
  - ../../base

commonLabels:
  environment: qa

replicas:
  - name: corp-banking-ui
    count: 2

patches:
  - target:
      kind: Deployment
      name: corp-banking-ui
    patch: |-
      - op: add
        path: /spec/template/spec/containers/0/env
        value:
          - name: ENVIRONMENT
            value: "qa"
  - target:
      kind: ConfigMap
      name: corp-banking-ui-config
    patch: |-
      - op: add
        path: /data/ENVIRONMENT
        value: "qa"
