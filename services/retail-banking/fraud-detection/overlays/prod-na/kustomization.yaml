apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: retail-banking

bases:
  - ../../base

resources:
  - rollout.yaml

patches:
  - target:
      kind: Deployment
      name: fraud-detection
    patch: |-
      - op: remove
        path: /
  - target:
      kind: ConfigMap
      name: fraud-detection-config
    patch: |-
      - op: add
        path: /data/ENVIRONMENT
        value: "prod-na"

---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: fraud-detection
  labels:
    app: fraud-detection
    strategy: canary
spec:
  replicas: 5
  selector:
    matchLabels:
      app: fraud-detection
  template:
    metadata:
      labels:
        app: fraud-detection
    spec:
      containers:
      - name: fraud-detection
        image: docker.io/akshitmadanharness/fraud-detection:latest
        ports:
        - containerPort: 8093
        env:
        - name: ENVIRONMENT
          value: "prod-na"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "1Gi"
            cpu: "800m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8093
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8093
          initialDelaySeconds: 5
          periodSeconds: 5
  minReadySeconds: 30
  revisionHistoryLimit: 3
  strategy:
    canary:
      maxSurge: '25%'
      maxUnavailable: 0
      steps:
        - setWeight: 10
        - pause: { duration: 10m }
        - setWeight: 25
        - pause: { duration: 10m }
        - setWeight: 50
        - pause: { duration: 10m }
        - setWeight: 100

