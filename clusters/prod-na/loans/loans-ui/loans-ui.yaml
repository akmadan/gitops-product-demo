---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: loans-ui
  namespace: loans
  labels:
    app: loans-ui
    environment: prod-na
    strategy: blueGreen
spec:
  replicas: 5
  selector:
    matchLabels:
      app: loans-ui
  template:
    metadata:
      labels:
        app: loans-ui
        environment: prod-na
    spec:
      containers:
      - name: loans-ui
        image: docker.io/akshitmadanharness/loans-ui:latest
        ports:
        - containerPort: 80
        env:
        - name: ENVIRONMENT
          value: "prod-na"
        - name: REACT_APP_API_URL
          value: "https://loans-api.prod-na.svc.cluster.local"
        - name: REACT_APP_DOCUMENT_SERVICE
          value: "https://document-processing.prod-na.svc.cluster.local"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
  minReadySeconds: 30
  revisionHistoryLimit: 3
  strategy:
    blueGreen:
      activeService: loans-ui-active
      previewService: loans-ui-preview
      autoPromotionEnabled: false
      scaleDownDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: loans-ui-active
  namespace: loans
  labels:
    app: loans-ui
    environment: prod-na
spec:
  selector:
    app: loans-ui
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: loans-ui-preview
  namespace: loans
  labels:
    app: loans-ui
    environment: prod-na
spec:
  selector:
    app: loans-ui
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: loans-ui-config
  namespace: loans
  labels:
    app: loans-ui
    environment: prod-na
data:
  ENVIRONMENT: "prod-na"
  REACT_APP_API_URL: "https://loans-api.prod-na.svc.cluster.local"
  REACT_APP_DOCUMENT_SERVICE: "https://document-processing.prod-na.svc.cluster.local"
  REACT_APP_ENV: "production"
  REACT_APP_VERSION: "latest"
